ACME_ENABLE=false
ACME_EMAIL=xxx@domain.tld
ACME_DOMAINS=domain.tld,www.domain.tld

DOCKER_DOMAIN="dev.domain"

TRAEFIK_DEFAULT_ENTRYPOINTS=http
TRAEFIK_ENTRYPOINT_HTTP=--entryPoints="Name:http Address::80"
TRAEFIK_ENTRYPOINT_HTTPS=
TRAEFIK_HOST=domain.local,www.domain.local

NODE_ENV=dev

DB_CREDENTIALS
PORTS


# Créer un .env différent pour chaque branche qu'on veut envoyer sur le serveur

- Mettre en place Jenkinsfile
- deploy to dev and prod
- mettre en place BDD
- Expliquer tout ce que j'ai fait

## Environnements

- on a un enironnement de dev en local et on teste sur localhost
- on a un environnement de test sur le serveur --> test.fatboar.site + traefik https
- on a l'envronnement de prod sur le serveur --> fatboar.site + traefik https

Créer mon docker-compose.override.yml en local avec la config de dev 
Mettre la config de stage et prod et la pousser sur les branches stage et master
L'ajouter dans le gitignore. On fait toujors les modifs en local
